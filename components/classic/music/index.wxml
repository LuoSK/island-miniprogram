<view class='container'>
  <image class="img {{playing?'rotation':''}}" src="{{img}}" />
  <image bindtap="onPlay" class='player-img' src="{{!playing?playSrc:pauseSrc}}" />
  <image class='tag' src="{{tag}}" />
  <text class='content'>{{content}}</text>
</view>